apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: production
data:
  DATABASE_HOST: "postgres-service"
  DATABASE_PORT: "5432"
  DATABASE_NAME: "sass_platform1"
  FRONTEND_URL: "https://your-domain.com"
  NODE_ENV: "production"
